format = """
${sudo}$directory$git_branch
$python$gcloud 
[>](bold cyan) """

command_timeout = 1000

[python]
symbol = " "

[git_branch]
symbol = " "

[gcloud]
format = 'on [$symbol${project} as $account@$domain]($style) '
